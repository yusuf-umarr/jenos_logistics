enum RegisterState { success, error, loading, idle }
// enum UserAddressState { success, error, loading, idle }
enum UpdateUserState { success, error, loading, idle }
enum PaymentState { success, error, loading, idle }

enum NetworkState { success, error, loading, idle }

enum LoginState { success, error, loading, idle }
// enum UserDataState { success, error, loading, idle }

enum CartState { filled, error, empty }

enum Auth { signin, signup }
enum AccountType { individual, enterprise }


